const MUSCLE_GROUP_CHOICE = require('./muscle-group-choice');
const ERROR = require('./error');
const START_WORKOUT = require('./start-workout');

module.exports = {
  MUSCLE_GROUP_CHOICE,
  START_WORKOUT,
  ERROR
};
