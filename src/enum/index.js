const SET_MY_COMMANDS_PAYLOADS = require('./set-my-commands-payloads');
const WORKOUT_DESCRIPTIONS = require('./workout-type-description');
const MUSCLE_GROUPS = require('./muscle-group');
const COMMANDS = require('./commands');
const EVENTS = require('./bot-events');

module.exports = {
  SET_MY_COMMANDS_PAYLOADS,
  WORKOUT_DESCRIPTIONS,
  MUSCLE_GROUPS,
  COMMANDS,
  EVENTS
};
